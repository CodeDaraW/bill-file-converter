# Web

Web UI is intentionally not implemented in v1.

The current rewrite focuses on the importable Go `core` package and the first-class CLI. A future local-only web UI can be built on top of `core` without changing the parsing pipeline.
